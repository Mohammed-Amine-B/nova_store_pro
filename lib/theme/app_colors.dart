import 'package:flutter/material.dart';

/// "Souk" palette — warm, market-inspired colors instead of a generic
/// blue-on-white SaaS look. Grounded in the shop's own context.
class AppColors {
  AppColors._();

  // Light theme
  static const lightBackground = Color(0xFFFBF9F6);
  static const lightForeground = Color(0xFF14201F);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightPrimary = Color(0xFF0E7C7B); // Souk Teal
  static const lightPrimaryForeground = Color(0xFFFCFEFF);
  static const lightPrimarySoft = Color(0xFFDCF0EF);
  static const lightSecondary = Color(0xFFF3EFE7);
  static const lightSecondaryForeground = Color(0xFF2A3230);
  static const lightMuted = Color(0xFFF4F1EA);
  static const lightMutedForeground = Color(0xFF6B7370);
  static const lightAccent = Color(0xFFFCEED2); // Saffron soft
  static const lightAccentForeground = Color(0xFF7A4E0A);
  static const lightSaffron = Color(0xFFF2A93B);
  static const lightDestructive = Color(0xFFE4572E); // Terracotta red
  static const lightDestructiveForeground = Color(0xFFFFFBF8);
  static const lightSuccess = Color(0xFF16A34A);
  static const lightSuccessForeground = Color(0xFFFAFCFE);
  static const lightWarning = Color(0xFFF2994A);
  static const lightWarningForeground = Color(0xFF2E1D06);
  static const lightBorder = Color(0xFFE6E0D4);
  static const lightInput = Color(0xFFDDD5C4);
  static const lightSidebar = Color(0xFFFFFFFF);
  static const lightSidebarForeground = Color(0xFF223231);

  // Dark theme
  static const darkBackground = Color(0xFF0E1614);
  static const darkForeground = Color(0xFFF3F7F6);
  static const darkCard = Color(0xFF152220);
  static const darkPrimary = Color(0xFF3FBDBA);
  static const darkPrimaryForeground = Color(0xFF0B1615);
  static const darkSecondary = Color(0xFF1E2C29);
  static const darkSecondaryForeground = Color(0xFFF3F7F6);
  static const darkMuted = Color(0xFF1E2C29);
  static const darkMutedForeground = Color(0xFF93A19D);
  static const darkAccent = Color(0xFF2B2418);
  static const darkAccentForeground = Color(0xFFF2C879);
  static const darkSaffron = Color(0xFFF2A93B);
  static const darkDestructive = Color(0xFFEF6B4C);
  static const darkDestructiveForeground = Color(0xFFF3F7F6);
  static const darkSuccess = Color(0xFF34C577);
  static const darkSuccessForeground = Color(0xFF0B1615);
  static const darkWarning = Color(0xFFF2994A);
  static const darkWarningForeground = Color(0xFF2E1D06);
  static const darkBorder = Color(0x1FFFFFFF);
  static const darkInput = Color(0x29FFFFFF);
  static const darkSidebar = Color(0xFF152220);
  static const darkSidebarForeground = Color(0xFFF3F7F6);
}